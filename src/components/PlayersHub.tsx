import { HPBar } from "./HPBar";
import { PlayersContainer } from "./PlayersContainer";

export const PlayersHub = () => {
    return(
        <>
            <HPBar />
            <PlayersContainer />
        </>
    );
}